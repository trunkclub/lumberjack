{
  "name": "a11y-report",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "app": "cd app && yarn start",
    "develop": "concurrently \"npm run server\" \"npm run app\"",
    "server": "node server/index.js",
    "start": "npm run server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "combine-reports": "node utils/axe/combineReports.js --testID=$TESTID",
    "check-routes": "node utils/axe/checkRoutes.js --testID=$TESTID"
  },
  "author": "Beck Kramer",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.4.2",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "axe-core": "^3.1.2",
    "axe-puppeteer": "^1.0.0",
    "concurrently": "^4.1.0",
    "mkdirp": "^0.5.1",
    "moment": "^2.24.0",
    "placeholders": "^1.0.0",
    "puppeteer": "^1.12.2",
    "yargs": "^13.2.1"
  }
}
